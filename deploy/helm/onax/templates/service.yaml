apiVersion: v1
kind: Service
metadata:
  name: {{ include "onax.fullname" . }}
  labels:
    {{- include "onax.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "onax.selectorLabels" . | nindent 4 }}
