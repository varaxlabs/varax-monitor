---
# Source: onax/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: onax
  labels:
    helm.sh/chart: onax-0.1.0
    app.kubernetes.io/name: onax
    app.kubernetes.io/instance: onax
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
---
# Source: onax/templates/clusterrole.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: onax
  labels:
    helm.sh/chart: onax-0.1.0
    app.kubernetes.io/name: onax
    app.kubernetes.io/instance: onax
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
rules:
  # Read CronJobs
  - apiGroups: ["batch"]
    resources: ["cronjobs"]
    verbs: ["get", "list", "watch"]

  # Read Jobs (to track executions)
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "list", "watch"]
---
# Source: onax/templates/clusterrolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: onax
  labels:
    helm.sh/chart: onax-0.1.0
    app.kubernetes.io/name: onax
    app.kubernetes.io/instance: onax
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: onax
subjects:
  - kind: ServiceAccount
    name: onax
    namespace: monitoring
---
# Source: onax/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: onax
  labels:
    helm.sh/chart: onax-0.1.0
    app.kubernetes.io/name: onax
    app.kubernetes.io/instance: onax
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app.kubernetes.io/name: onax
    app.kubernetes.io/instance: onax
---
# Source: onax/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: onax
  labels:
    helm.sh/chart: onax-0.1.0
    app.kubernetes.io/name: onax
    app.kubernetes.io/instance: onax
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: onax
      app.kubernetes.io/instance: onax
  template:
    metadata:
      labels:
        app.kubernetes.io/name: onax
        app.kubernetes.io/instance: onax
    spec:
      serviceAccountName: onax
      securityContext:
        fsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: onax
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
          image: "ghcr.io/varaxlabs/onax:1.0.0"
          imagePullPolicy: IfNotPresent
          args:
            - --metrics-bind-address=:8080
            - --health-probe-bind-address=:8081
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            limits:
              cpu: 100m
              memory: 128Mi
            requests:
              cpu: 50m
              memory: 64Mi
---
# Source: onax/templates/servicemonitor.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: onax
  labels:
    helm.sh/chart: onax-0.1.0
    app.kubernetes.io/name: onax
    app.kubernetes.io/instance: onax
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
  namespaceSelector:
    matchNames:
      - monitoring
  selector:
    matchLabels:
      app.kubernetes.io/name: onax
      app.kubernetes.io/instance: onax
