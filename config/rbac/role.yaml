apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: onax
  labels:
    app: onax
rules:
  - apiGroups: ["batch"]
    resources: ["cronjobs"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "list", "watch"]
