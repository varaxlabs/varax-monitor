name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          docker build -t ghcr.io/kubeshield/k8s-cronjob-monitor:${VERSION} .
          docker tag ghcr.io/kubeshield/k8s-cronjob-monitor:${VERSION} ghcr.io/kubeshield/k8s-cronjob-monitor:latest
          docker push ghcr.io/kubeshield/k8s-cronjob-monitor:${VERSION}
          docker push ghcr.io/kubeshield/k8s-cronjob-monitor:latest

      - name: Build release binaries
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o k8s-cronjob-monitor-linux-amd64 cmd/operator/main.go
          GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -ldflags="-s -w" -o k8s-cronjob-monitor-linux-arm64 cmd/operator/main.go
          GOOS=darwin GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o k8s-cronjob-monitor-darwin-amd64 cmd/operator/main.go
          GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -ldflags="-s -w" -o k8s-cronjob-monitor-darwin-arm64 cmd/operator/main.go

      - name: Package Helm chart
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          helm package deploy/helm/k8s-cronjob-monitor --version ${VERSION} --app-version ${VERSION}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true
          files: |
            k8s-cronjob-monitor-*
            k8s-cronjob-monitor-*.tgz
